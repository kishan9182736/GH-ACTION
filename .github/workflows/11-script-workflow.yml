name: Script_Workflow

on:
  workflow_dispatch:

jobs:
  script-workflow:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        

      - name: Script workflow
        run: |
          echo "Helloooo, this is script workflow"

      - name: Run Python Script
        run: python script.py
